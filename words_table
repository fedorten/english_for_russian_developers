# 🐍 Самые частые слова в Python для новичков

## 🔑 Ключевые слова языка

### Условные операторы
- **`if`** (если) - проверка условия
- **`else`** (иначе) - альтернативное выполнение  
- **`elif`** (иначе если) - дополнительные условия
- **`True`/`False`** (истина/ложь) - логические значения

### Циклы
- **`for`** (для) - цикл по элементам последовательности
- **`while`** (пока) - цикл с условием
- **`break`** (прервать) - выход из цикла
- **`continue`** (продолжить) - переход к следующей итерации
- **`in`** (в) - проверка принадлежности

### Функции и модули
- **`def`** (определить) - объявление функции
- **`return`** (вернуть) - возврат значения
- **`import`** (импортировать) - подключение модулей
- **`from`** (из) - импорт конкретных объектов
- **`as`** (как) - псевдоним для импорта

### Классы и ООП
- **`class`** (класс) - создание класса
- **`self`** (сам) - ссылка на экземпляр класса
- **`__init__`** (инициализация) - конструктор класса

## 🔧 Встроенные функции

### Базовые операции
- **`print()`** - вывод в консоль
- **`input()`** - ввод с клавиатуры  
- **`len()`** - длина объекта
- **`type()`** - тип объекта
- **`id()`** - идентификатор объекта

### Преобразование типов
- **`int()`** - в целое число
- **`str()`** - в строку
- **`float()`** - в дробное число
- **`list()`** - в список
- **`dict()`** - в словарь
- **`set()`** - в множество
- **`tuple()`** - в кортеж
- **`bool()`** - в логический тип

### Математические функции
- **`abs()`** - модуль числа
- **`round()`** - округление
- **`sum()`** - сумма элементов
- **`min()`** - минимальное значение
- **`max()`** - максимальное значение
- **`range()`** - генерация последовательности

### Работа с коллекциями
- **`sorted()`** - сортировка
- **`enumerate()`** - нумерация элементов
- **`zip()`** - объединение коллекций

## 📋 Методы строк

- **`.lower()`** - к нижнему регистру
- **`.upper()`** - к верхнему регистру  
- **`.strip()`** - удаление пробелов
- **`.split()`** - разделение на список
- **`.join()`** - соединение списка в строку
- **`.replace()`** - замена подстроки
- **`.find()`** - поиск подстроки
- **`.format()`** - форматирование строки

## 📊 Методы списков

- **`.append()`** - добавление в конец
- **`.pop()`** - удаление по индексу
- **`.remove()`** - удаление по значению
- **`.insert()`** - вставка по индексу
- **`.sort()`** - сортировка
- **`.reverse()`** - обратный порядок
- **`.count()`** - подсчет элементов
- **`.index()`** - поиск индекса

## 📚 Методы словарей

- **`.keys()`** - получение ключей
- **`.values()`** - получение значений
- **`.items()`** - получение пар ключ-значение
- **`.get()`** - безопасное получение значения
- **`.update()`** - обновление словаря

## ⚠️ Обработка ошибок

- **`try`** - блок для перехвата ошибок
- **`except`** - обработка исключения
- **`finally`** - выполняется всегда
- **`raise`** - вызов исключения

## 🔄 Логические операторы

- **`and`** (и) - логическое И
- **`or`** (или) - логическое ИЛИ  
- **`not`** (не) - логическое НЕ
- **`is`** (есть) - проверка идентичности
- **`in`** (в) - проверка вхождения

## 💡 Специальные значения

- **`None`** - пустое значение
- **`Ellipsis`** (или `...`) - многоточие
- **`NotImplemented`** - не реализовано
